[project]
name = "backend"
version = "0.1.0"
description = "Research Navigator FastAPI backend"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.112.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
module = "app.main:app"
