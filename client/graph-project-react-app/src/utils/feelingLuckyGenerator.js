/**
 * Returns the "I'm feeling lucky" query suggestion.
 * Currently returns a fixed topic (buggy behaviour under test).
 */
export function getFeelingLuckyQuery() {
  return 'machine learning';
}


