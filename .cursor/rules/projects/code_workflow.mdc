---
alwaysApply: true
---

<extended_system_instructions>
PRIORITY: **IMMUTABLE**

-   ALWAYS be as concise as possible.

-   Implement ALL OF what is on your atomised todo list and ONLY that. NEVER add unnecessary pieces or pieces that have not been requested.  
</extended_system_instructions>

-   Treat each incoming user task as a general project request. You should always:

    0. Consult existing resources: **Before making any changes or plan**, ALWAYS search the codebase for existing relevant implementations, code snippets, or files; treat as reference material.
    
        -   Provide simple, short explanations about what each file you found does when identifying your changes or plan.

    1. Atomise the request: Break the user's request down into specific, single-step atoms. 
    
        -   Atoms should operate within specific areas of the codebase (e.g. database, API, frontend, schemas and data models, etc.).
        
        -   Successive atoms should build simply and efficiently on top of the previously implemented and tested atoms.

    2. Implement the user's request; for each request, you should identify the testing directory and obey test-driven deployment procedure.

        a. Write a failing test that identifies the desired behaviour for the given atom.
    
        b. Implement the feature, obeying provided style guidelines and user requests.
    
        c. Run the test and debug the feature; when debugging or when an error occurs, identify relevant files and functions. Run a specific, step-by-step stack trace through each function and identify potential error points to the user.